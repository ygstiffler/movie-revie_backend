NODE_ENV=development
PORT=5000

# MongoDB connection string (Railway Mongo plugin or external cluster)
MONGO_URL=mongodb://127.0.0.1:27017/movie-review

# Authentication secrets
JWT_SECRET=replace-with-strong-secret
GOOGLE_CLIENT_ID=replace-with-google-client-id

# Frontend origin allowed by CORS
FRONTEND_URL=http://localhost:3000

# Optional: comma-separated list of extra origins for CORS
CORS_ADDITIONAL_ORIGINS=

# Automatically provided by Railway (documented for reference)
# RAILWAY_PUBLIC_DOMAIN=

